# 02a_corr_furnace/

**EN:** Correlation analysis — furnace-wide. All 4 passes pooled, all cycles pooled. Spearman ρ for each driver vs `dd_abs_max`. Phase-binned (5-day windows) to separate causal from operator-feedback effects.

**RU:** Корреляционный анализ — вся печь. Все 4 пасса объединены, все циклы объединены. Спирмен ρ для каждого драйвера vs `dd_abs_max`. Разбивка по 5-дневным фазам для разделения причинных эффектов от операторской обратной связи.

**Expected findings / Ожидаемые результаты:** run_age(+), feed_C5+(+), COT(+), steam_hc(−)

**Output / Выход:** `output/{FURNACE}/02a_corr_furnace/`
