# 01_tags/

**EN:** Tag inspection and analysis window detection. Reads from clean parquet. Checks each tag role for flat lines, spikes, data gaps. Confirms run boundaries are correct. Flags issues for DCS verification.

**RU:** Проверка тегов и определение окон анализа. Читает из чистого parquet. Проверяет каждую роль тегов на плоские линии, спайки, пропуски данных. Подтверждает правильность границ прогонов. Помечает проблемы для проверки DCS.

**Output / Выход:** `output/{FURNACE}/01_tags/`
