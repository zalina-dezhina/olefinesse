# 03_trajectories/

**EN:** Delta-Delta trajectory analysis. Plots `dd_abs_max` vs `run_age_days` for every cycle. Overlays all runs to show natural variation. Compares runs with different operating conditions to isolate driver effects on trajectory shape.

**RU:** Анализ траекторий Delta-Delta. График `dd_abs_max` vs `run_age_days` для каждого цикла. Наложение всех прогонов для оценки естественной вариации. Сравнение прогонов с разными условиями для изоляции эффекта драйверов.

**Output / Выход:** `output/{FURNACE}/03_trajectories/`
