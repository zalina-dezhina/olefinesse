# 02b_corr_passes/

**EN:** Correlation analysis — per pass. Runs after 02a. Uses `dd_abs_max_A/B/C/D` separately. Question: does one pass consistently coke faster? Does steam imbalance predict which pass degrades first?

**RU:** Корреляционный анализ — по пассам. Запускается после 02a. Использует `dd_abs_max_A/B/C/D` отдельно. Вопрос: один пасс коксует быстрее? Предсказывает ли дисбаланс пара какой пасс деградирует первым?

**Output / Выход:** `output/{FURNACE}/02b_corr_passes/`
